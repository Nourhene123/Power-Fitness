import { Routes } from '@angular/router';
import { BmiComponent } from './bmi.component';

export const BMI_ROUTES: Routes = [
  { path: '', component: BmiComponent, title: 'Free BMI Calculator | Power Fitness' },
];
